# K3 RISC-V 具身智能系统 — 三个月开发计划（自研算子版）

> **排期方式**：单线串行推进（不假设其他人力配合）
> **技术路线**：**自研算子**（matmul 已有，其余自己写），非官方栈开箱
> **交付目标**：一条可演示的端到端垂直 Demo + 关键瓶颈报告

---

## 0. 主要任务总结

| # | 任务 | 现状 / 说明 |
|---|---|---|
| 1 | **SmolVLA 复现（Linux）** | 开源可控，作为基线 |
| 2 | **StarryOS 移植到 K3** | 已有基础移植，**缺部分驱动**，缺哪些未知，需跑起来再排查、缺啥补啥 |
| 3 | **自研算子补齐** | 现仅 `matmul`，Transformer 全套算子需自己写 |
| 4 | **模型推理移植** | Octos Agent / World Model / VLM 算子适配与推理 |
| 5 | **30B 推理** | **按自研算子估算** → 见下方风险说明 |
| 6 | **跨板通信** | K3 ↔ RK3576，多模态感知/规划/控制协同 |
| 7 | **OminiX-API 全链路** | ASR / LLM / TTS / 视觉 |
| 8 | **测试与交付** | 功能/稳定性/性能 + 文档三件套 |

> dora-rs RISC-V 移植已由他人完成，不占本计划工期。

### ⚠️ 关于 30B（重点标注）
本计划的 30B **按自研算子路线估算**。matmul 之外的全套算子（含 Flash-attention、FP8 量化、KV-Cache 优化）都要自己写并调精度。**结论：30B 的深度自研优化大概率无法在三个月内完成**，列为 **stretch goal / 高风险项**，主推目标锁定在 **≤7B 模型自研算子跑通**。若强行主攻 30B，其余任务将大幅牺牲。

---

## 阶段 A｜基线与摸底（第 1–2 周）

| 任务 | 预估 |
|---|---|
| SmolVLA Linux 复现，跑通训练/推理基线 | 1–2 周 |
| StarryOS 上板跑起来，**排查缺失驱动清单**（串口/中断/内存控制器/AI 核接口/外设） | 与上并行摸底 |

**里程碑 M1**：SmolVLA 基线出结果 + StarryOS 缺失驱动清单明确

> 📌 缺失驱动清单是本阶段最重要的产出——直接决定阶段 B 的真实工作量。清单出来前，驱动补齐工期为**模糊估计**。

---

## 阶段 B｜核心攻坚（第 3–7 周，最长阶段，主关键路径）

| 任务 | 预估 |
|---|---|
| StarryOS 按清单补齐关键驱动，保证系统稳定运行 | 2–4 周（模糊，视清单） |
| AI 核接口驱动打通（供算子调用） | 视清单而定 |
| 补齐 Transformer 全套算子：Softmax / RMSNorm / RoPE / KV-Cache / SiLU-SwiGLU / 残差 / Embedding / 量化-反量化 | 4–6 周（含精度对齐，若踩精度问题可到 8 周） |
| 逐层精度对齐，定位崩塌点 | 贯穿本阶段 |
| ≤7B 模型端到端推理跑通 | 阶段末 |

**里程碑 M2**：StarryOS 关键驱动补齐可稳定运行 + ≤7B 模型自研算子推理成功、精度对齐

> ⚠️ 自研算子是全局瓶颈。matmul 只占可用推理内核约 10–20% 工作量，Attention 融合、量化、KV-Cache 往往比 matmul 更耗时。

---

## 阶段 C｜系统集成（第 7–10 周）

| 任务 | 预估 |
|---|---|
| K3 ↔ RK3576 跨板通信链路打通 | 1.5–2 周 |
| OminiX-API 全链路（ASR/LLM/TTS/视觉，单模块各 3–5 天） | 2–3 周 |
| 跨芯片协同调度（感知→规划→控制） | 压在集成后期 |

**里程碑 M3**：端到端链路 + 跨板协同可演示

---

## 阶段 D｜测试与交付（第 10–12 周）

| 任务 | 预估 |
|---|---|
| 功能验证（全链路） | 1 周 |
| 瓶颈定位（内核调度 / 推理延迟 / 跨板通信） | 1–1.5 周 |
| 稳定性/性能压测（**仅主链路**，非全量） | 1 周 |
| 文档三件套（系统移植文档 / 模型优化日志 / 芯片适配白皮书，边做边攒） | 末周收尾 |

**里程碑 M4（最终交付）**：K3+StarryOS 演示样机 + 文档 + 瓶颈报告

---

## 1. 主要时间花费一览

| 任务块 | 主要耗时 | 风险 |
|---|---|---|
| SmolVLA 复现 | 1–2 周 | 低 |
| StarryOS 驱动排查+补齐 | 3–6 周（含摸底） | **中高（缺啥未知）** |
| **自研算子补齐** | **4–8 周** | **高（主瓶颈）** |
| ≤7B 模型移植+精度对齐 | 与算子并行 | 中 |
| 跨板通信 | 1.5–2 周 | 低 |
| OminiX 全链路 | 2–3 周 | 中 |
| 测试+文档 | 2–3 周 | 中 |
| **30B 自研深度优化** | — | **极高，大概率期内做不完 → stretch** |

> 两大耗时黑洞：**自研算子**与 **StarryOS 驱动补齐**。串行推进下，两者叠加就已接近三个月上限，30B 深度优化基本挤不进来。

---

## 2. 里程碑时间线

| 里程碑 | 时间 | 交付物 |
|---|---|---|
| M1 | 第 2 周末 | SmolVLA 基线 + StarryOS 缺失驱动清单 |
| M2 | 第 7 周末 | 驱动补齐可稳定运行 + ≤7B 模型自研算子推理成功 |
| M3 | 第 10 周末 | 端到端链路 + 跨板协同可演示 |
| M4 | 第 12 周末 | 演示样机 + 文档三件套 + 瓶颈报告 |

---

## 3. 三个月能到什么程度（现实预期）

### ✅ 大概率完成
- SmolVLA Linux 复现
- StarryOS 关键驱动补齐、可稳定运行（前提：缺失量可控）
- 自研算子跑通 ≤7B 模型，精度对齐
- K3↔RK3576 跨板通信
- OminiX 端到端链路可演示
- 主链路瓶颈定位 + 文档

### ⚠️ 大概率降级 / 做不到
- **30B 自研算子深度优化**（列为 stretch，大概率不完成）
- 全量稳定性/性能压测（仅覆盖主链路）
- 全部多模态模型完整适配（视算子/驱动进度取舍）

---

## 一句话总评

> 单线串行、自研算子路线，三个月能交付**"StarryOS 补齐 + ≤7B 模型自研算子跑通 + 端到端 Demo + 瓶颈报告"**。**30B 深度自研优化按此路线基本超期，务必作为 stretch，不占主线工期。** 两大风险：自研算子（主瓶颈）与 StarryOS 缺失驱动量（缺啥未知）。
